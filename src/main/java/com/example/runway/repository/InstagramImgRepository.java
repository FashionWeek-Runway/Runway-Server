package com.example.runway.repository;

import com.example.runway.domain.InstagramImg;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstagramImgRepository extends JpaRepository<InstagramImg,Long> {
}
