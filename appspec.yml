version: 0.0
os: linux
files:
  - source:  /
    destination: /var/www/dev
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/dev.sh
      timeout: 60
      runas: ubuntu
